import type { HigherLowerPublicState, RoomView } from '@pokemon-universe/shared'; import { RotateCcw, StopCircle, Trophy } from 'lucide-react';
export function HigherLowerResults({ room, selfId, onLobby, onEnd }: { room: RoomView; selfId: string; onLobby(): void; onEnd(): void }) { const game = room.game as HigherLowerPublicState; const host = room.hostId === selfId; return <section className="mx-auto max-w-2xl px-5 py-12"><div className="card"><Trophy className="mx-auto text-electric" size={52} /><h1 className="mb-6 text-center font-display text-4xl">Clasificación Higher or Lower</h1><div className="space-y-2">{game.results?.standings.map((entry) => <div key={entry.playerId} className="rounded-2xl bg-surface-raised p-3"><strong>{entry.position}. {room.members.find((m) => m.id === entry.playerId)?.displayName}</strong><strong className="float-right text-berry">{entry.points} pts</strong><p className="text-sm font-bold text-ink/50">{entry.stats.correct} aciertos · {entry.stats.sameCorrect} SAME · mejor racha {entry.stats.bestStreak} · {entry.stats.accuracy}%</p></div>)}</div>{host ? <div className="mt-6 flex justify-center gap-3"><button className="btn-primary" onClick={onLobby}><RotateCcw /> Volver al lobby</button><button className="btn-ghost" onClick={onEnd}><StopCircle /> Terminar</button></div> : <p className="mt-6 text-center">Esperando al host…</p>}</div></section>; }
