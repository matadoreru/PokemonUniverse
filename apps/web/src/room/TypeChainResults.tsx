import type { RoomView, TypeChainPublicState } from '@pokemon-universe/shared';
import { Link2, RotateCcw, StopCircle } from 'lucide-react';
import { Avatar } from '../components/Avatar';

export function TypeChainResults({ room, selfId, onLobby, onEnd }: { room: RoomView; selfId: string; onLobby(): void; onEnd(): void }) {
  const game = room.game as TypeChainPublicState; const host = room.hostId === selfId;
  return <section className="mx-auto max-w-4xl px-3 py-8 sm:px-5"><div className="rounded-2xl border border-ink/10 bg-surface/95 p-4 shadow-card sm:p-7"><Link2 className="mx-auto text-aqua" size={52} /><span className="label mt-2 text-center">Type Chain — Resultados</span><h1 className="mb-2 text-center font-display text-3xl sm:text-4xl">Clasificación final</h1><p className="mb-6 text-center font-bold text-ink/65">Cadena más larga: {game.longestChain} Pokémon</p><div className="space-y-2.5">{game.results?.standings.map((entry) => { const member = room.members.find((candidate) => candidate.id === entry.playerId); return <article key={entry.playerId} className={`grid items-center gap-3 rounded-2xl border p-3 sm:grid-cols-[auto_auto_1fr_auto] ${entry.position === 1 ? 'border-electric/50 bg-electric/[0.08]' : 'border-ink/10 bg-surface-raised/70'}`}><strong className="w-9 text-center font-display text-2xl">{entry.position <= 3 ? ['🥇', '🥈', '🥉'][entry.position - 1] : entry.position}</strong><Avatar name={member?.displayName ?? entry.playerId} avatar={member?.avatar} size="md" /><div className="min-w-0"><strong className="block truncate font-display text-xl">{member?.displayName ?? entry.playerId}</strong><p className="text-sm font-bold text-ink/65">{entry.stats.validSubmissions} válidos · {entry.stats.invalidAttempts} fallos · {entry.stats.turnsSurvived} turnos</p><small className="font-extrabold text-berry">{entry.stats.timeoutEliminations} eliminaciones por timeout</small></div><strong className="justify-self-end text-xl text-berry">{entry.points} pts</strong></article>; })}</div>{host ? <div className="mt-7 flex flex-wrap justify-center gap-3"><button className="btn-primary" onClick={onLobby}><RotateCcw /> Volver al mismo lobby</button><button className="btn-ghost" onClick={onEnd}><StopCircle /> Terminar sesión</button></div> : <p className="mt-6 text-center font-bold text-ink/65">Esperando a que el host vuelva al lobby…</p>}</div></section>;
}
