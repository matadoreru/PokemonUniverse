import type { RoomView, ZoomedPokemonPublicState } from '@pokemon-universe/shared';
import { RotateCcw, Search, StopCircle } from 'lucide-react';
import { Avatar } from '../components/Avatar';

export function ZoomedPokemonResults({ room, selfId, onLobby, onEnd }: { room: RoomView; selfId: string; onLobby(): void; onEnd(): void }) {
  const game = room.game as ZoomedPokemonPublicState; const host = room.hostId === selfId;
  return <section className="mx-auto max-w-4xl px-3 py-8"><div className="rounded-[2rem] border border-ink/10 bg-surface/95 p-4 shadow-card sm:p-7"><Search className="mx-auto text-aqua" size={50} /><span className="label mt-2 text-center">Zoomed Pokémon</span><h1 className="mb-6 text-center font-display text-4xl">Clasificación final</h1><div className="space-y-2">{game.results?.standings.map((entry) => { const member = room.members.find((item) => item.id === entry.playerId); const correct = entry.stats.correct ?? 0; return <article key={entry.playerId} className={`grid grid-cols-[auto_auto_1fr_auto] items-center gap-3 rounded-2xl border p-3 ${entry.position === 1 ? 'border-electric/50 bg-electric/[.08]' : 'border-ink/10 bg-ink/[.03]'}`}><strong className="w-9 text-center font-display text-2xl">{entry.position <= 3 ? ['🥇', '🥈', '🥉'][entry.position - 1] : entry.position}</strong><Avatar name={member?.displayName ?? entry.playerId} avatar={member?.avatar} size="md" /><div className="min-w-0"><strong className="block truncate font-display text-xl">{member?.displayName ?? entry.playerId}</strong><p className="text-sm font-bold text-ink/45">{correct} Pokémon · {entry.stats.firstPositions ?? 0} primeras posiciones · {entry.stats.firstTry ?? 0} first guesses · {entry.stats.maxZoomSolves ?? 0} a máximo zoom</p></div><strong className="text-xl text-berry">{entry.points} pts</strong></article>; })}</div>{host ? <div className="mt-7 flex flex-wrap justify-center gap-3"><button className="btn-primary" onClick={onLobby}><RotateCcw /> Volver al mismo lobby</button><button className="btn-ghost" onClick={onEnd}><StopCircle /> Terminar sesión</button></div> : <p className="mt-6 text-center font-bold text-ink/45">Esperando al host…</p>}</div></section>;
}
