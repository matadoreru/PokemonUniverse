import type { PokeTabooPublicState, RoomView } from '@pokemon-universe/shared';
import { Mic2, RotateCcw, StopCircle } from 'lucide-react';
import { Avatar } from '../components/Avatar';

export function PokeTabooResults({ room, selfId, onLobby, onEnd }: { room: RoomView; selfId: string; onLobby(): void; onEnd(): void }) {
  const game = room.game as PokeTabooPublicState; const host = room.hostId === selfId;
  return <section className="mx-auto max-w-4xl px-3 py-8 sm:px-5"><div className="rounded-2xl border border-ink/10 bg-surface p-4 shadow-card sm:p-7"><Mic2 className="mx-auto text-berry" size={50} /><span className="label mt-2 text-center">PokéTaboo · Resultados</span><h1 className="mb-2 text-center font-display text-3xl sm:text-4xl">Así ha ido la partida</h1><p className="mx-auto mb-6 max-w-xl text-center font-bold text-ink/60">Puntos por descubrir Pokémon y por ayudar al grupo a encontrarlos.</p><div className="space-y-2.5">{game.results?.standings.map((entry) => { const member = room.members.find((candidate) => candidate.id === entry.playerId); return <article key={entry.playerId} className={`grid items-center gap-3 rounded-xl border p-3 sm:grid-cols-[auto_auto_1fr_auto] ${entry.position === 1 ? 'border-electric/45 bg-electric/[.07]' : 'border-ink/10 bg-surface-raised/70'}`}><strong className="w-9 text-center font-display text-2xl">{entry.position <= 3 ? ['🥇', '🥈', '🥉'][entry.position - 1] : entry.position}</strong><Avatar name={member?.displayName ?? entry.playerId} avatar={member?.avatar} size="md" /><div className="min-w-0"><strong className="block truncate font-display text-xl">{member?.displayName ?? entry.playerId}</strong><p className="text-sm font-bold text-ink/65">{entry.stats.guessedPokemon ?? 0} adivinados · {entry.stats.firstTry ?? 0} al primer intento · {entry.stats.descriptorSuccesses ?? 0}/{entry.stats.descriptorRounds ?? 0} turnos de descriptor exitosos</p><div className="mt-1 flex flex-wrap gap-1.5 text-xs font-extrabold"><span className="rounded-full bg-leaf/10 px-2 py-0.5 text-leaf">+{entry.stats.pointsFromGuessing ?? 0} adivinando</span><span className="rounded-full bg-aqua/10 px-2 py-0.5 text-aqua">+{entry.stats.pointsFromDescribing ?? 0} describiendo</span></div></div><strong className="justify-self-end text-xl text-berry">{entry.points} pts</strong></article>; })}</div>{host ? <div className="mt-7 flex flex-wrap justify-center gap-3"><button className="btn-primary" onClick={onLobby}><RotateCcw /> Continuar sesión</button><button className="btn-ghost" onClick={onEnd}><StopCircle /> Terminar sesión</button></div> : <p className="mt-6 text-center font-bold text-ink/65">Esperando a que el host continúe la sesión…</p>}</div></section>;
}
