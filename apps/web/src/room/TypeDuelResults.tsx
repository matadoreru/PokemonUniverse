import type { RoomView, TypeDuelPublicState } from '@pokemon-universe/shared'; import { RotateCcw, StopCircle, Swords } from 'lucide-react';
export function TypeDuelResults({ room, selfId, onLobby, onEnd }: { room: RoomView; selfId: string; onLobby(): void; onEnd(): void }) { const game = room.game as TypeDuelPublicState; const host = room.hostId === selfId; return <section className="mx-auto max-w-2xl px-5 py-12"><div className="card"><Swords className="mx-auto text-berry" size={52} /><h1 className="mb-6 text-center font-display text-4xl">Clasificación Type Duel</h1>{game.results?.standings.map((entry) => <div key={entry.playerId} className="mb-2 rounded-2xl bg-surface-raised p-3"><strong>{entry.position}. {room.members.find((m) => m.id === entry.playerId)?.displayName}</strong><strong className="float-right text-berry">{entry.points} pts</strong><p className="text-sm font-bold text-ink/50">{entry.stats.duelsWon}/{entry.stats.duelsPlayed} victorias · {entry.stats.winRate}% · {entry.stats.incorrectAttempts} fallos · media {((entry.stats.averageCorrectTimeMs ?? 0) / 1000).toFixed(1)}s</p></div>)}{host ? <div className="mt-6 flex justify-center gap-3"><button className="btn-primary" onClick={onLobby}><RotateCcw /> Lobby</button><button className="btn-ghost" onClick={onEnd}><StopCircle /> Terminar</button></div> : <p className="mt-6 text-center">Esperando al host…</p>}</div></section>; }
